const defaultState = {
  inputValue: 'Input something',
  dataList: [
    'Today',
    'Tomorrow',
    'The day after tomorrow'
  ]
}
export default(state = defaultState, action) => {
  return state
}